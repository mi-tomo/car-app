json.array! @model do |product|
  json.id product.id
  json.model product.model
  # json.exhaust product.exhaust
  # json.modelyear product.modelyear
  # json.color product.color
  # json.distance product.distance
  # json.evaluation product.evaluation
end